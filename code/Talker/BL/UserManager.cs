using System;
using Talker.DAL;

namespace Talker.BL
{
    /*
	public static class UserManager
	{
        static readonly IUserService mService;

		static UserManager ()
		{
			mService = new UserService();
		}

		public static bool IsThisUserExisted (string pName, string pPassword)
		{
			return mService.IsThisUserExisted (pName, pPassword);
		}

		public static void SaveUser (User pUser)
		{
			mService.SaveUser (pUser);
		}

		public static void SaveUser (string pName, string pPassword)
		{
			User one = new User (pName, pPassword);
			mService.SaveUser (one);
		}
	}
 */   
}

