using System;
using System.Collections.Generic;
using Talker.DAL;

namespace Talker.BL
{/*
	public static class MessageManager
	{
		static IMessageDatabase mDB;

		static MessageManager()
		{
			mDB = UserService.GetMessageDB ();
		}

		public static void SaveMessage (Message pItem)
		{
			mDB.SaveMessage (pItem);
		}

		public static Message GetMessage (int pMessageId)
		{
			return mDB.GetMessage (pMessageId);
		}

		public static void DeleteMessage (Message pItem)
		{
			mDB.DeleteMessage (pItem);
		}

		public static List<Message> GetMessages(int pUserId)
		{
			return mDB.GetMessages (pUserId);
		}
	}
 */   
}

